
(library
  (name topdown)
  (libraries core_kernel str fmt lib reporting core effect)
  (inline_tests)
  (preprocess (pps ppx_jane ppx_deriving.map ppx_deriving.eq ppx_deriving.fold ppx_deriving.create))
)
