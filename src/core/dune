(library
  (name core)
  (libraries core_kernel str fmt qcheck ocamlgraph lib reporting mtl)
  (inline_tests)
  (preprocess (pps ppx_jane ppx_deriving.map ppx_deriving.show ppx_deriving.eq ppx_deriving.fold ppx_deriving.create))
)