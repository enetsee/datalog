(library
  (name compiler)
  (libraries core_kernel str fmt qcheck lib reporting effect parser source core partition dataflow adorn typecheck)
  (inline_tests)
  (preprocess (pps ppx_jane ppx_variants_conv ppx_deriving.map ppx_deriving.show ppx_deriving.eq ppx_deriving.fold ppx_deriving.create))
)