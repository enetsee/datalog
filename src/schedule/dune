(library
  (name schedule)
  (libraries core_kernel str fmt qcheck lib reporting effect tree graph_util core constraint dependency)
  (inline_tests)
  (preprocess (pps ppx_jane ppx_variants_conv ppx_deriving.map ppx_deriving.show ppx_deriving.eq ppx_deriving.fold ppx_deriving.create))
)